// First JS file - What is up doggigity
(function myFunc() {
    
    var priorities = [];
    var p = priorities;
    p.push("Tunnel to the server");
    p.push("Animation testing");
    p.push("Physics");
    p.push("Ensuring hit detection?");
    p.push("Ok, good. Once we get that all done, we can start focussing on the rest of the reqs");
    
    var gripes = [];
    var g = gripes;
    g.push("Hitting tab to finish auto complete, bad idea.");
    g.push("Correct. Use enter instead");
    g.push("Autocomplete is including all of the tokens in the code, including the text!");
    g.push("But I like enter to make a new line! I'm lazy");
    g.push("Enter for autocomplete = minor problem. Incomplete list of autocomplete options = BIG ASS problem");
    g.push("When changing variables it pops up autocomplete!");
    g.push("Valid point.");
    
    var x = {};
    x.foo = function() {};
    x.foo();
})();
